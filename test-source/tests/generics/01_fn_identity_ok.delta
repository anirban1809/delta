function identity<T>(value: T): T {
    return value;
}

function main(): uint8 {
    let x: int32 = identity<int32>(42);
    let y: char = identity<char>('2');
    if (x != 42) { return uint8(1); }
    return uint8(0);
}
