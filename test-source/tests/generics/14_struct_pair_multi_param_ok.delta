type struct Pair<T, U> = { first: T, second: U };

function main(): uint8 {
    const p: Pair<int32, bool> = Pair<int32, bool> { first: 7, second: true };
    if (p.first != 7) { return uint8(1); }
    if (!p.second)    { return uint8(2); }
    return uint8(0);
}
