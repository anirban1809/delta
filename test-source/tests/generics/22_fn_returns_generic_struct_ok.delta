type struct Box<T> = { value: T };

function wrap<T>(x: T): Box<T> {
    return Box<T> { value: x };
}

function main(): uint8 {
    const b: Box<int32> = wrap(5);
    if (b.value != 5) { return uint8(1); }
    return uint8(0);
}
