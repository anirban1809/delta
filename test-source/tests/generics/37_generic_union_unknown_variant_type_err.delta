type union nullable<T> = T | null;

type struct listNode = {
    value: int32,
    next: owned<listNode>,
};

type struct linkedList = {
    start: listNode,
};

function main(): uint8 {
    return 0;
}
