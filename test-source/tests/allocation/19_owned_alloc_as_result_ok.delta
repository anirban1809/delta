type struct AllocError = { };
type struct Payload = { count: int32 };

function main(): uint8 {
    const payload = new Payload { count: 4 } as result;
    check result {
        return uint8(1);
    }
    const count = payload.count;
    return uint8(count);
}
