interface Readable {
    function value(): int32;
}

type struct number_box = {
    stored: int32
} implements Readable;

function (box: &number_box) value(): int32 {
    return box.stored;
}

function main(): uint8 {
    const box = number_box { stored: 7 };
    return uint8(box.value());
}

