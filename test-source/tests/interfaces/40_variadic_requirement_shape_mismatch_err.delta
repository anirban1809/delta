interface Batch {
    function append<...T>(...values: T): void;
}

type struct batch = {
} implements Batch;

function (value: &batch) append<T>(values: T[]): void {
}

function main(): uint8 {
    return uint8(0);
}
