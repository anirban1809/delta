import {
    Readable,
    constant_reader,
    make_reader
} from "./external/readers/readers.ffi";

function read_value<R: Readable>(reader: &R): int32 {
    return reader.value();
}

function main(): uint8 {
    const reader: constant_reader = make_reader(23);
    if (read_value(reader) != 23) {
        return 1;
    }
    return 0;
}

