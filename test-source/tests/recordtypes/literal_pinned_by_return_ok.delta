type Vec3 = { x: float64; y: float64; z: float64; };

function origin(): Vec3 {
    return { x: 0.0, y: 0.0, z: 0.0 };
}

function main(): int32 {
    const o: Vec3 = origin();
    if (int32(o.x + o.y + o.z) != 0) { return 1; }
    return 0;
}
