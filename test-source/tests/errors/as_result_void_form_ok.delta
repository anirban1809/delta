type struct OverflowError = { };

function ensure(x: int32): void | OverflowError {
    const y = x + 1 as result;
    check result {
        return error as { };
    }
    return;
}

function main(): int8 {
    ensure(5) as result;
    check result {
        return int8(1);
    }
    return int8(0);
}
