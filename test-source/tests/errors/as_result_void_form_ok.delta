type struct OverflowError = { };

function ensure(x: int32): void | OverflowError {
    const y = x + 1 as result;
    check result {
        return error as { };
    }
    return;
}

function main(): uint8 {
    ensure(5) as result;
    check result {
        return uint8(1);
    }
    return uint8(0);
}
