type Counter = { value: int32; };

function (c: edit &Counter) increment() {
    c.value = c.value + 1;
}

function main(): uint8 {
    let c: Counter = { value: 0 };
    c.increment();
    return uint8(c.value);
}
