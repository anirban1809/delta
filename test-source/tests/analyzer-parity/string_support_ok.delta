// Task 6: string type + string literals must be supported.
// Currently ResolveType("string") is invalid and StringLiteral returns invalid.
function greet(): string {
    return "hello";
}

function main(): uint8 {
    return uint8(0);
}
