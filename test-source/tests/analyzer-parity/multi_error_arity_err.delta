// Each error path returns one variant, so two error values are an arity error.
type ErrA = { a: bool; };
type ErrB = { b: bool; };

function f(): int32 | ErrA, ErrB {
    return error as { a: true }, { b: true };
}

function main(): int8 {
    return 0;
}
