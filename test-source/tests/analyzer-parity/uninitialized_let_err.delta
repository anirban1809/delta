// Task 10: reading a `let` binding before it is assigned must error as
// uninitialized. Declaration without an initializer should itself be allowed.
function main(): uint8 {
    let x: int32;
    return x;
}
