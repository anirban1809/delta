// An explicit `unique type` transfers with `move`. Expects pass.
unique type Handle = { id: int32; };

function consume(value: Handle) { }

function main(): uint8 {
    let value: Handle = { id: 7 };
    consume(move value);
    return uint8(0);
}
