// Whole-value reassignment revives a moved `let` binding. Expects pass.
unique type Handle = { id: int32; };

function consume(value: Handle) { }

function main(): int8 {
    let value: Handle = { id: 7 };
    consume(move value);
    value = { id: 8 };
    return int8(value.id);
}
