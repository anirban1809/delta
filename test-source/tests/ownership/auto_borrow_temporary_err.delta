// MVP auto-borrow needs an addressable storage path, not a call temporary (inspect(makeCounter())). Expects fail ("temporary").
type Counter = { value: int32; };

function makeCounter(): Counter { return { value: 7 }; }
function inspect(counter: &Counter): int32 { return counter.value; }

function main(): int8 {
    return inspect(makeCounter());
}
