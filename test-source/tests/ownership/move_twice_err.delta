// A binding cannot be moved twice. Expects fail ("moved").
unique type Handle = { id: int32; };

function consume(value: Handle) { }

function main(): uint8 {
    let value: Handle = { id: 7 };
    consume(move value);
    consume(move value);
    return 0;
}
