// Moving a borrow (`&Handle` parameter) never transfers ownership of its referent. Expects fail ("borrow").
unique type Handle = { id: int32; };

function consume(value: Handle) { }

function invalid(value: &Handle) {
    consume(move value);
}

function main(): uint8 { return 0; }
