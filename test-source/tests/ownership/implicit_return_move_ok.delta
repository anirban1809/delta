// Returning a non-Copyable by-value parameter implicitly transfers it. Expects pass.
unique type Handle = { id: int32; };

function identity(value: Handle): Handle {
    return value;
}

function main(): int8 {
    let value: Handle = { id: 7 };
    const result = identity(move value);
    return int8(result.id);
}
