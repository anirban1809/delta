// A bare Unique argument cannot satisfy a by-value parameter. Expects fail ("unique").
unique type Handle = { id: int32; };

function consume(value: Handle) { }

function main(): uint8 {
    let value: Handle = { id: 7 };
    consume(value);
    return 0;
}
