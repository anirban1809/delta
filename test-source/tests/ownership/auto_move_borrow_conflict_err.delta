// Moving and auto-borrowing the same root in one call conflict. Expects fail ("move").
unique type Handle = { id: int32; };

function conflict(owner: Handle, reader: &Handle) { }

function main(): uint8 {
    let value: Handle = { id: 7 };
    conflict(move value, value);
    return 0;
}
