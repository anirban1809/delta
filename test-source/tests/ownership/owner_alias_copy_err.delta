// Aliases inherit ownership/Copyable capability (BoxAlias = Box); bare copy rejected. Expects fail ("non-copyable").
type Box = { value: heap<int32>; };
type BoxAlias = Box;

function main(): int32 {
    const a: BoxAlias = { value: new 1 };
    const b = a;
    return 0;
}
