// Copyable values are also Cloneable; explicit clone is legal (and redundant). Expects pass.
type Point = { x: int32; y: int32; };

function main(): int32 {
    const original: Point = { x: 1, y: 2 };
    const duplicate = clone original as result;
    check result { return 1; }
    return duplicate.x;
}
