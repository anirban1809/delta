// Borrowing lets a caller use an Owned/Unique value without transferring it. Expects pass.
unique type Handle = { id: int32; };

function inspect(value: &Handle): int32 { return value.id; }

function main(): uint8 {
    const value: Handle = { id: 7 };
    return uint8(inspect(value));
}
