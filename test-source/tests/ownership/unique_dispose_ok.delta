// An exact `edit &T`, no-arg, void dispose hook is legal on an explicit `unique type`. Expects pass.
unique type Handle = { id: int32; };

function (value: edit &Handle) dispose() {
    value.id = -1;
}

function main(): int8 {
    const value: Handle = { id: 7 };
    return int8(0);
}
