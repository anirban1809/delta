type struct Board = { cells: int32[3] };

function main(): uint8 {
    let b: Board = Board { cells: [1, 2, 3] };
    b.cells[0] = 99;
    if (b.cells[0] != 99) { return uint8(1); }
    return uint8(0);
}
