type struct Sprite = { pos: int32[2], visible: bool };

function main(): uint8 {
    const s: Sprite = Sprite { pos: [16, 32], visible: true };
    if (s.pos[0] != 16) { return uint8(1); }
    if (!s.visible)     { return uint8(2); }
    return uint8(0);
}
