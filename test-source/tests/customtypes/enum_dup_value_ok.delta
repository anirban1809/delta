type enum Alias = { A: 1, B: 1, C: 2 };

function main(): uint8 {
    if (int32(Alias.A) != 1) { return uint8(1); }
    if (int32(Alias.B) != 1) { return uint8(2); }
    if (Alias.A != Alias.B)  { return uint8(3); }
    if (Alias.C == Alias.A)  { return uint8(4); }
    return uint8(0);
}
