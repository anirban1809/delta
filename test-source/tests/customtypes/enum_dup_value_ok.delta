type enum Alias = { A: 1, B: 1, C: 2 };

function main(): int32 {
    if (int32(Alias.A) != 1) { return 1; }
    if (int32(Alias.B) != 1) { return 2; }
    if (Alias.A != Alias.B)  { return 3; }
    if (Alias.C == Alias.A)  { return 4; }
    return 0;
}
