type struct Circle = { r: float64 };
type struct Square = { s: float64 };

type union Shape = Circle | Square;

function main(): uint8 {
    const sh: Shape = Square { s: 3.0 };
    return uint8(0);
}