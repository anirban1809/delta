type enum Color = { Red, Green, Blue };

function main(): int32 {
    const c: Color = Color.Green;
    if (c != 1)          { return 1; }
    if (Color.Red != 0)  { return 2; }
    if (Color.Blue != 2) { return 3; }
    return 0;
}
