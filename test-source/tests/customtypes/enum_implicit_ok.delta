type enum Color = { Red, Green, Blue };

function main(): uint8 {
    const c: Color = Color.Green;
    if (c != 1)          { return uint8(1); }
    if (Color.Red != 0)  { return uint8(2); }
    if (Color.Blue != 2) { return uint8(3); }
    return uint8(0);
}
