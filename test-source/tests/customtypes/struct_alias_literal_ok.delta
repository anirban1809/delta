type byte_count = uintsize;

type struct dynamicstring = {
    data: string,
    length: uintsize,
    capacity: uintsize
};

type ds = dynamicstring;
type text = ds;

function new_str(s: string): dynamicstring {
    const str = new ds {
        data: s,
        length: s.length,
        capacity: s.length
    };

    return str;
}

function new_str_through_chain(s: string): dynamicstring {
    const str = new text {
        data: s,
        length: s.length,
        capacity: s.length
    };
    return str;
}

function main(): uint8 {
    let value = new_str("hello");
    let chained = new_str_through_chain("alias");
    if (value.length != uintsize(5)) { return uint8(1); }
    if (chained.length != uintsize(5)) { return uint8(2); }
    return uint8(0);
}
