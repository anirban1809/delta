type struct Vec3 = { x: int32, y: int32, z: int32 };
type Position    = Vec3;

function makePosition(): Position {
    return Vec3 { x: 8, y: 13, z: 21 };
}

function main(): uint8 {
    const value: Vec3 = makePosition();
    if (value.x != 8)  { return uint8(1); }
    if (value.y != 13) { return uint8(2); }
    if (value.z != 21) { return uint8(3); }
    return uint8(0);
}
