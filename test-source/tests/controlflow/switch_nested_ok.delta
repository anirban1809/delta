function main(): uint8 {
    let a: int32 = 1;
    let b: int32 = 2;
    switch (a) {
        case 1:
            switch (b) {
                case 2:   return uint8(0);
                default:  return uint8(1);
            }
        default: return uint8(2);
    }
}
