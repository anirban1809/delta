function main(): uint8 {
    let last: int32 = -1;
    for (let i: int32 = 0; i < 100; i++) {
        if (i == 5) { break; }
        last = i;
    }
    if (last != 4) { return uint8(1); }
    return uint8(0);
}