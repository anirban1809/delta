function main(): uint8 {
    let i: int32 = 0;
    let total: int32 = 0;
    for (; i < 5; i++) {
        total = total + i;
    }
    if (total != 10) { return uint8(1); }
    if (i != 5)      { return uint8(2); }
    return uint8(0);
}
