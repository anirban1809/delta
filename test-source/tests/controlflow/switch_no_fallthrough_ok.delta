function main(): uint8 {
    let counter: int32 = 0;
    let n: int32 = 1;
    switch (n) {
        case 1:   counter = counter + 10;
        case 2:   counter = counter + 100;
        default:  counter = counter + 1000;
    }
    if (counter != 10) { return uint8(1); }
    return uint8(0);
}
