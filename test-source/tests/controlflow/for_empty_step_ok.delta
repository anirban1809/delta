function main(): uint8 {
    let total: int32 = 0;
    for (let i: int32 = 0; i < 5; ) {
        total = total + i;
        i++;
    }
    if (total != 10) { return uint8(1); }
    return uint8(0);
}
