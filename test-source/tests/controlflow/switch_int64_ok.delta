function main(): uint8 {
    let n: int64 = 1_000_000_000_000;
    switch (n) {
        case 1_000_000_000_000:  return uint8(0);
        case 0:                   return uint8(1);
        default:                  return uint8(2);
    }
}
