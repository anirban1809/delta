function main(): uint8 {
    let i: int32 = 10;
    let a: int32 = i++;
    let b: int32 = i--;
    if (a != 10) { return uint8(1); }
    if (b != 11) { return uint8(2); }
    if (i != 10) { return uint8(3); }
    return uint8(0);
}
