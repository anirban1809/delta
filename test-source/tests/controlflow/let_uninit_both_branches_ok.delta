function pick(b: bool): int32 {
    let x: int32;
    if (b == true) { x = 10; } else { x = 20; }
    return x;
}

function main(): uint8 {
    if (pick(true)  != 10) { return uint8(1); }
    if (pick(false) != 20) { return uint8(2); }
    return uint8(0);
}
