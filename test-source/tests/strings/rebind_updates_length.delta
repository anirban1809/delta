function main(): uint8 {
    let name = "value1";
    if (name.length != uintsize(6)) { return uint8(1); }

    name = "value2-long";
    if (name.length != uintsize(11)) { return uint8(2); }

    return uint8(0);
}
