import unsafe { write } from "./unistd.ffi";

function main(): uint8 {
    const message = "Hello from Delta\n";
    const written = write(1, message, message.length);
    if (written < 0) {
        return 1;
    }
    return 0;
}
