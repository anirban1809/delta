function main(): uint8 {
    let tag: int32 = 2;
    switch (tag) {
        case 1:
            return uint8(1);
        case 2:
            return uint8(2);
        default:
            return uint8(3);
    }
}
