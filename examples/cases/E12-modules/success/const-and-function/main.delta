// E12 modules success — the entry module imports a const and a function by name.
import { BONUS, square } from "./mathlib";

function main(): int32 {
    const area: int32 = square(5);
    return area + BONUS;
}
