// Imports must precede any other top-level declaration (Phase I).
function helper(): int32 { return 1; }

import { ANSWER } from "./helper";   // ERROR: imports must precede other declarations

function main(): int32 {
    return ANSWER + helper();
}
