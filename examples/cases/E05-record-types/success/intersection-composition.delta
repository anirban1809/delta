// E05 success — intersection composition `Base & { extra }`.
type Animal = { legs: int32; sound: int32; };
type Cat = Animal & { indoor: bool; };

function main(): int32 {
    const felix: Cat = { legs: 4, sound: 2, indoor: true };
    return felix.legs;
}
