// E05 success — assign a field through a mutable `let` binding.
type Vec3 = { x: float64; y: float64; z: float64; };

function main(): int8 {
    let p: Vec3 = { x: 1.0, y: 2.0, z: 3.0 };
    p.x = 10.0;
    return int32(p.x);
}
