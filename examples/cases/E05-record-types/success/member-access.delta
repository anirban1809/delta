// E05 success — read a field with member access `.`.
type Vec3 = { x: float64; y: float64; z: float64; };

function main(): int32 {
    const p: Vec3 = { x: 1.0, y: 2.0, z: 3.0 };
    const x: float64 = p.x;
    return int32(x);
}
