// E0506 — duplicate field `x` in record `Vec2`
type Vec2 = { x: int32; x: int32; };   // ERROR[E0506]: duplicate field `x` in record `Vec2`

function main(): uint8 {
    const p: Vec2 = { x: 1 };
    return p.x;
}
