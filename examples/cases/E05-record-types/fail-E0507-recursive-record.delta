// E0507 — recursive record `Loop` requires indirection
// hint: wrap the field in `heap<T>` to break the cycle
type Loop = { self: Loop; };   // ERROR[E0507]: recursive record `Loop` requires indirection

function main(): int32 {
    return 0;
}
