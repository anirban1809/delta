// E0902 — `new` operand has type `bool`, expected `int64`
type Counter = { value: heap<int64>; };

function main(): int8 {
    let a: Counter = { value: new true };   // ERROR[E0902]: `new` operand has type `bool`, expected `int64`
    return 0;
}
