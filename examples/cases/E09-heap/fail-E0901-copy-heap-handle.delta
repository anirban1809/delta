// E0901 — `heap<int64>` is move-only; copying the handle would alias the allocation
type Counter = { value: heap<int64>; };

function main(): uint8 {
    let a: Counter = { value: new 0 };
    let b: Counter = { value: a.value };   // ERROR[E0901]: `heap<int64>` is move-only; copying the handle would alias the allocation
    return 0;
}
