// E0206 — operator `&&` expects `bool` operands, found `int32`
function main(): int32 {
    const a: int32 = 6;
    const b: int32 = 4;
    const both: bool = a && b;   // ERROR[E0206]: operator `&&` expects `bool` operands, found `int32`
    return 0;
}
