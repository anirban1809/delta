// E0207 — unary `-` expects a numeric operand, found `bool`
function main(): int32 {
    const flag: bool = true;
    const n: bool = -flag;   // ERROR[E0207]: unary `-` expects a numeric operand, found `bool`
    return 0;
}
