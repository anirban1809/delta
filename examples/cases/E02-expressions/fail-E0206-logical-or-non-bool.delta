// E0206 — operator `||` expects `bool` operands, found `int32`
function main(): uint8 {
    const a: int32 = 6;
    const b: int32 = 4;
    const either: bool = a || b;   // ERROR[E0206]: operator `||` expects `bool` operands, found `int32`
    return 0;
}
