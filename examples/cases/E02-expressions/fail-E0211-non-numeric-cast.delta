// E0211 — `bool(x)` is not a valid conversion: `bool` is not a numeric type
function main(): uint8 {
    const n: int32 = 1;
    const flag: bool = bool(n);   // ERROR[E0211]: `bool(x)` is not a valid conversion: `bool` is not a numeric type
    return 0;
}
