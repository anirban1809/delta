// E1104 — `Counter` has no method `reset`
type Counter = { value: heap<int64>; };

function (c: &Counter) get(): int64 {
    return c.value;
}

function main(): uint8 {
    let c: Counter = { value: new 0 };
    c.reset();   // ERROR[E1104]: `Counter` has no method `reset`
    return 0;
}
