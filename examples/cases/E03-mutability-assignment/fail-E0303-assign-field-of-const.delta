// E0303 — cannot assign field of `const` binding `p`
type Vec2 = { x: int32; y: int32; };

function main(): int8 {
    const p: Vec2 = { x: 1, y: 2 };
    p.x = 10;   // ERROR[E0303]: cannot assign field of `const` binding `p`
    return p.x;
}
