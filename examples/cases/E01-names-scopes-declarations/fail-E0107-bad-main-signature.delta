// E0107 — `main` must be declared at top level as `function main(): int32`
function main(): float64 {   // ERROR[E0107]: `main` must be declared at top level as `function main(): int32`
    return 0.0;
}
