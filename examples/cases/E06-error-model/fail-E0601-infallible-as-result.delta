// E0601 — `as result` requires a fallible expression — this expression cannot fail
function main(): uint8 {
    const x: int32 = 5;
    const ok = x as result;   // ERROR[E0601]: `as result` requires a fallible expression — this expression cannot fail
    check result {
        return 1;
    }
    return ok;
}
