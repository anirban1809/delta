// E0209 — condition must be `bool`, found `int32`
function main(): uint8 {
    const n: int32 = 1;
    if (n) {   // ERROR[E0209]: condition must be `bool`, found `int32`
        return 1;
    }
    return 0;
}
