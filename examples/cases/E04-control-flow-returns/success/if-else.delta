// E04 success — `if` / `else if` / `else` with bool conditions and braced bodies.
function classify(n: int32): int32 {
    if (n > 0) {
        return 1;
    } else if (n < 0) {
        return int32(0) - 1;
    } else {
        return 0;
    }
}

function main(): int8 {
    return classify(5);
}
