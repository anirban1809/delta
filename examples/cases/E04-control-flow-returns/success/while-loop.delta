// E04 success — `while` loop with a bool condition and a fresh block scope.
function main(): uint8 {
    let sum: int32 = 0;
    let i: int32 = 0;
    while (i < 5) {
        sum += i;
        i++;
    }
    return sum;
}
